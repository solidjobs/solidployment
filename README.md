# Solidployment: A Docker solution for SolidJobs deployment

This repository is made for make easier deployments in SolidJobs. Now each docker container is running on their own, so we want to register the containers in Docker Hub and run them here.

